name: Sync Claude Code Docs Archive

on:
  workflow_dispatch:
  schedule:
    - cron: "42 * * * *"

permissions:
  contents: write

jobs:
  sync:
    uses: f-pisani/claudecodedocs/.github/workflows/snapshot-sync.yml@ff187f72d6d610797cf0096a53790818a563ef01
    with:
      source_url: https://code.claude.com/docs/llms.txt
      site_name: Claude Code Docs
      site_url: https://code.claude.com
      repo_title: Claude Code Docs Archive
      schedule_label: Hourly at :42 UTC
      tool_ref: ff187f72d6d610797cf0096a53790818a563ef01
    secrets: inherit
